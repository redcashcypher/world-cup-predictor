Drop Golden Boot "meme" media here as .webp or .png, referenced from
`src/data/goldenBoot.mock.json` via the `mediaUrl` field, e.g.:

  /media/mbappe-hattrick.webp

Until real assets are added, the Media modal shows a wireframe
placeholder frame instead of a broken image.
